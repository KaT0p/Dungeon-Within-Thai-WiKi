# แนวทางการ Commit โปรเจกต์

ให้อ่านและปฏิบัติตามไฟล์นี้ทุกครั้งก่อนสร้าง Git commit ในโปรเจกต์นี้

## รูปแบบข้อความ Commit

ข้อความ commit ทุกครั้งต้องขึ้นต้นด้วย tag ภาษาไทยหนึ่งรายการจากรายการต่อไปนี้เท่านั้น

- `[ เพิ่ม ]` สำหรับการเพิ่มฟีเจอร์ ไฟล์ เนื้อหา หรือความสามารถใหม่
- `[ ลบ ]` สำหรับการลบฟีเจอร์ ไฟล์ เนื้อหา หรือโค้ด
- `[ แก้ไข ]` สำหรับการแก้บั๊ก ข้อมูลผิด หรือพฤติกรรมที่ทำงานไม่ถูกต้อง
- `[ ปรับปรุง ]` สำหรับการปรับ UX/UI ประสิทธิภาพ โครงสร้างโค้ด หรือสิ่งที่มีอยู่ให้ดีขึ้น

ให้เว้นหนึ่งช่องหลัง tag แล้วตามด้วยคำอธิบายภาษาไทยที่สั้นและชัดเจน โดยใช้รูปแบบ:

```text
[ tag ] คำอธิบายสิ่งที่เปลี่ยนแปลง
```

ตัวอย่าง:

```text
[ เพิ่ม ] หน้ารวมข้อมูลมอนสเตอร์
[ ลบ ] รูปภาพที่ไม่ได้ใช้งาน
[ แก้ไข ] ลิงก์หน้ารายละเอียดจอมมาร
[ ปรับปรุง ] การแสดงผลบนอุปกรณ์มือถือ
```

หากหนึ่ง commit มีการเปลี่ยนแปลงหลายประเภท ให้เลือก tag ตามวัตถุประสงค์หลักของ commit และหลีกเลี่ยงการรวมงานที่ไม่เกี่ยวข้องกันไว้ใน commit เดียว

ก่อน commit ให้ตรวจ staged changes และยืนยันว่าข้อความ commit ใช้หนึ่งในสี่ tag ข้างต้นเสมอ ห้ามใช้ข้อความ commit ที่ไม่มี tag
